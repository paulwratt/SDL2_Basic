# SDL2_Basic
This is a fork of [SDL_BASIC](https://github.com/dashxdr/SDL_basic) that also includes an `sdl1` branch. The patched update of `sdl1` branch in maintanied from [here](https://github.com/paulwratt/pistol_Basic) that keeps it inline with the original after the SDL2 transition.

# Usage
Read the [README](README).
